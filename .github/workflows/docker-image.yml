name: Build the codecanvas-up image

on:
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the codecanvas-up image
      run: docker build . --file Dockerfile --tag codecanvas-up:$(date +%s)
